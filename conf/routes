# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                          controllers.Application.index()
GET     /Promotion                   controllers.Application.Promotion()
GET     /Products                   controllers.Application.Product()
GET     /Rabbit                     controllers.Application.Rabbit()

GET     /from                           controllers.Application.inputproduct()
POST    /showfrom                      controllers.Application.showproduct()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)

